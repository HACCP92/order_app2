import 'package:flutter/material.dart';
import 'main_screen.dart';

class HeroP extends StatelessWidget {
  const HeroP({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
